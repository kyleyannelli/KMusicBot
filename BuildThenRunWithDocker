#!/bin/zsh
docker build -t kmusic_bot_main .
# check if script was run with -d flag
if [[ $1 == "-d" ]]; then
    docker run -d --restart always kmusic_bot_main
else
    docker run kmusic_bot_main
fi
